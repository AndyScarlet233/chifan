# 吃饭 · 使用手册

> 一个命令行小工具：从 Z-Library（Librella）搜索并下载电子书。
> 只依赖 Python 标准库（3.8+），无需安装任何第三方包，不需要浏览器自动化。
>
> 支持多账号号池：批量下载时一个账号当日额度用尽、或被服务端限流卡住，自动切换下一个账号接着下同一本书。

---

## 一、功能一览

- **搜索**：关键词搜书，返回候选列表（书名/作者/年份/格式/大小）。
- **单本下载**：自动匹配最佳候选，落盘前本地去重。
- **批量书单**：一个 txt 一行一本书名，跑完给统计。
- **多账号号池**：`accounts.json` 存多个账号，`--account` 指定、`--rotate` 自动轮换。
- **额度检查**：下载前查当日剩余额度，不会盲目浪费请求。
- **自动去重**：按归一化书名与目标目录比对，同书不重复下载。
- **域名探测**：镜像域名失效自动探测切换，结果缓存 15 分钟。
- **零依赖**：只用 Python 标准库。

## 二、环境要求

- Python 3.8 及以上（仅标准库）。
- 一个可用的 Z-Library 账号（免费号每日 10 本，会员号每日 20 本，以站点实际为准）。

## 三、凭据准备（三选一）

凭据是两个值：`remix_userid` 和 `remix_userkey`（登录后浏览器 Cookie 里可见）。

| 方式 | 做法 | 优先级 |
|---|---|---|
| 环境变量 | `ZLIB_USERID` / `ZLIB_USERKEY` | 最高（临时换号好用） |
| 号池 | `accounts.json`（见第五节，推荐） | 有 default 时次之 |
| 客户端配置 | Z-Library 桌面客户端 `%APPDATA%/z-library/config.json` | 兜底 |
| 单账号文件 | 仓库根目录自建 `credential.json`：`{"remix_userid":"…","remix_userkey":"…"}` | 兜底 |

> ⚠️ **`remix_userkey` 等同于账号密码**。它只应存在你自己的机器上；
> `accounts.json`、`credential.json` 已列入 `.gitignore`，**永远不要提交、不要外发**。

## 四、快速开始

```bash
PY=python                       # 或你的 Python 3.8+ 绝对路径
S=scripts/zlib_cli.py

# 1) 看账号与今日额度（输出「今日 2 / 10」＝ 已用 2 本、还剩 8 本）
$PY $S quota
$PY $S quota --all              # 看号池里每个账号

# 2) 搜索，只列结果不下载（不消耗额度）
$PY $S search "逻辑学导论" --limit 15

# 3) 先看会不会命中，不消耗额度
$PY $S download "理想国 柏拉图" --dry-run

# 4) 下载匹配度最高的一本
$PY $S download "逻辑学导论 第15版" --ext epub --out books

# 5) 批量书单（每行一个书名，# 开头是注释）——推荐带 --rotate
$PY $S batch wishlist.txt --out books --ext epub,pdf --rotate

# 6) 镜像域名失效时重新探测
$PY $S domains --refresh
```

常用参数：`--ext epub`（偏好格式）、`--lang zh`（偏好语言）、`--index 3`（选第 N 个候选）、
`--account 别名`（指定账号）、`--rotate`（多账号轮换）、`--force`（跳过去重与额度检查，**慎用**）、
`--json`（机器可读输出）。

**退出码**：`0` 成功 / `1` 网络或接口异常 / `2` 未找到或置信度不足 / `3` 当日额度用尽 / `4` 目标已存在同书。

## 五、多账号号池

号池文件 `accounts.json`（放在仓库根目录，与脚本同层；首次使用需自建）：

```json
{
  "default": "main",
  "accounts": [
    { "alias": "main", "userid": "10000001", "userkey": "…", "name": "我的主号", "note": "会员" },
    { "alias": "a2",   "userid": "10000002", "userkey": "…", "name": "小号",     "note": "免费" }
  ]
}
```

- `alias`：别名，命令里用 `--account 别名` 引用；
- `name`：账号名（随手记，便于辨认）；
- `note`：备注（会员/免费等）；
- 可选 `daily_limit`：手写覆盖该号每日上限。**`0` 是合法值＝停用这个号**（轮换时直接跳过）。

### 管理命令

```bash
$PY $S accounts list [--probe]                 # 列出号池；--probe 逐个查额度与会员状态
$PY $S accounts add a2 --userid U --userkey K --note "免费"
$PY $S accounts capture a3                     # 从桌面客户端当前登录状态抓号入池（Windows）
$PY $S accounts default main                   # 设默认号
$PY $S accounts remove a2                      # 删号
```

### 查重规则

- 同一个 `userid` 已挂在**别的别名**下 → 拒绝重复添加（`userid` 才是账号唯一标识，账号名可以重名）；
- 同一别名重复添加 → 视为更新。

### 选号优先级

1. 命令里给了 `--account 别名` → 用它；
2. 没给，但号池里有 `default` → 用 default；
3. 号池为空 → 回落到环境变量 / 客户端配置 / `credential.json`。

### 轮换语义（`batch --rotate`）

1. 每条书单先用**当前账号**查额度：额度用尽 → 切下一个账号；
2. 换号后用**新账号**对同一本书重新搜索＋取直链；若也报"无直链"（限流），继续下移；
3. 绕号池一圈仍拿不到 → 该书记为失败（`全账号风控/无直链`）；
4. 全部账号额度耗尽 → 打印 `[中止] 所有账号额度均已用尽`，退出码 `3`；
5. 结束时打印「各账号下载数: main=7 a2=10 …」。

## 六、额度判定规则（重要）

站点接口对**会员号**返回的 `downloads_limit` 往往是旧的免费值 `10`，服务器实际按会员额度放行；
而**免费号**返回的 `10` 就是真值。因此本工具按下面优先级判定有效额度：

1. 号池里该账号的 `daily_limit`（若手写了）；
2. 全局 `config.json` 的 `daily_limit`，**仅当该账号是会员时**才套用；
3. 接口返回值。

混合会员/免费账号时，用 `quota --all` 核对，别只看单个号的数字。

## 七、配置文件 `config.json`

```json
{
  "library_dir": "books",
  "filename_template": "{title}{author_part}.{ext}",
  "domains": ["…镜像域名列表，按优先级排序…"],
  "timeout": 30,
  "daily_limit": null
}
```

- `library_dir`：`--out` 未指定时的默认保存目录；
- `daily_limit`：全局额度覆盖，`null`＝用接口返回值；只对会员号生效（见第六节）；
- `domains`：镜像域名探测顺序，失效时可自行增删或用 `domains --refresh`。

## 八、文件命名与去重

- 文件名按 `filename_template` 生成，超长书名会截断到 110 字符——**扩展名永远保留**；
- 下载前按归一化书名与目标目录比对，命中即跳过（退出码 `4`），不浪费额度。

## 九、搜索避坑（实测经验）

1. **先 `--dry-run` 再正式下**。中文书名极易撞名：
   - 搜 `理想国` 会命中「理想国译丛」出版品牌，要搜 `理想国 柏拉图`；
   - 搜 `逻辑学导论` 会混入多个版次，要带版次：`逻辑学导论 第15版`。
2. **最稳做法：`download "精确书名 作者名"` 不带 `--index`**，命中后核对输出的书名/作者/年份。
3. **`--index N` 与 `search` 列表的序号不是一套**，照抄会下错书。
4. **教辅/法考/公考类务必带年份**：这类书大量是多年前的旧版，法律与大纲会过时，下了等于白占额度。
5. **注意置信度闸门偏宽松**：评分函数给"体积正常"的书有保底加分，所以"未找到/置信度不足"很少触发——
   自动化场景务必靠精确书名或 `--dry-run` 兜底，别指望脚本自己拦。
6. `--ext epub,pdf` 这样写是"加权"不是"排除"；写 `--ext epub` 会明显压低其它格式的分。

## 十、接口内幕（排障用）

- **鉴权靠 Cookie**：请求头带 `Cookie: remix_userid=…; remix_userkey=…`。只发 `remix-userid/userkey`
  请求头时，下载接口会返回"登录到您的账户"。
- **搜索必须 POST**：`POST /eapi/book/search`，body 为 `message=<关键词>`。同样的参数用 **GET** 会返回
  400"未找到请求的书"——这是最容易误判成"无结果"的坑。
- **取直链**：`GET /eapi/book/{id}/{hash}/file` 返回 CDN 直链（带 `expires`，过期需重新申请）。
  **这一步扣当日额度**；失败（无直链）不扣。
- **额度查询**：`GET /eapi/user/profile/{id}/{key}` 返回 `downloads_today / downloads_limit / isPremium`。
- **不要走的路**：网页路径 `/dl/…` 与 `/s/…` 会被 Cloudflare 挑战页拦下，走 EAPI 即可。
- 域名探测：`GET /eapi/info/ok`，结果缓存 15 分钟（`domains --refresh` 强制重探）。

## 十一、排障速查

| 现象 | 原因 | 处理 |
|---|---|---|
| 所有域名不可用 | 镜像变更/网络问题 | `domains --refresh`；检查代理 |
| 搜索"未找到"但库里有 | 用了 GET | 脚本已用 POST，若手改代码勿改回 |
| 下载接口"登录到您的账户" | 只发了头没发 Cookie | 带上完整 Cookie |
| 下载 0 字节 | 直链过期 | 重新调 `/file` 接口取新链接 |
| 命中同名书但版本不对 | 书名撞名 | 先 `--dry-run`，再 `--index N` 或精确书名 |
| 批量跑到一半全报无直链 | 该账号被限流 | 加 `--rotate`，或等次日换号 |
| 额度显示与实际不符 | 接口对会员返回旧值 | 见第六节；用 `quota --all` 核对 |

## 十二、安全与免责

- `userkey` 等同账号密码：只存本机，不提交仓库、不截图、不外发。
- 本工具仅用于**个人学习与备份**用途；请尊重作者与出版社的权益，
  下载内容的传播与使用请遵守你所在地区的法律法规。
- 仓库不包含、也永远不会包含任何账号凭据。
