# 使用手册

这个项目现在分成两个入口：

- `scripts/zlib_agent.py`：推荐给 ZCode/AI Agent，默认 EPUB，只负责检索和候选排序，不自动下载。
- `scripts/zlib_cli.py`：原项目兼容 CLI，保留旧功能，适合用户本人在本地手动使用。

## 一、ZCode 推荐用法

仓库已经包含：

```text
.zcode/skills/zlibrary-books/SKILL.md
```

把本仓库作为 ZCode 工作区打开后，进入“设置 → 技能”，点击刷新并启用 `zlibrary-books`。

之后可以直接说：

```text
帮我找《三体》的 EPUB，列出最可靠的几个版本供我选择。
```

Agent 会调用：

```bash
python scripts/zlib_agent.py "三体 刘慈欣" --json
```

Linux/macOS 如果只有 `python3`，换成：

```bash
python3 scripts/zlib_agent.py "三体 刘慈欣" --json
```

## 二、为什么 Agent 使用单独入口

原 `zlib_cli.py` 是一个功能较全的历史脚本，其中为了兼容部分镜像，曾主动关闭 TLS 证书校验，而且普通文本搜索会显示 userkey 的一小段。

给 ZCode 使用的 `zlib_agent.py` 会在任何网络请求发生前强制把 TLS 上下文恢复为 Python 默认安全配置，并且自己输出搜索结果，不调用旧脚本会泄露密钥片段的文本输出。

因此建议：

- AI 搜索只使用 `zlib_agent.py`
- 不让 Agent 自动调用旧 CLI 的下载功能
- 不让 Agent 自动使用 `--force`
- 不让 Agent 自动使用账号轮换

## 三、默认 EPUB

ZCode 安全检索入口默认：

```text
--ext epub
```

不需要额外写参数。

如果用户明确要求 PDF：

```bash
python scripts/zlib_agent.py "书名 作者" --ext pdf --json
```

如果想比较其他格式，可以显式指定对应扩展名。

## 四、搜索结果

`--json` 会返回适合 AI 读取的候选信息，包括：

- `rank`：排序位置
- `score`：匹配分
- `title`：书名
- `author`：作者
- `year`：年份
- `publisher`：出版社
- `language`：语言
- `extension`：文件格式
- `filesizeString`：文件大小
- `isbn`：如果 API 有返回
- `id` / `hash`：eAPI 图书标识

AI 不应只根据 `score` 选书。更可靠的判断顺序通常是：书名、作者、版次/年份、出版社/译者、语言、格式、文件大小，最后再参考分数。

教材、译著、合集和同名书最容易误判，应优先把多个候选交给用户选。

## 五、内置域名

候选域名位于 `config.json`：

```json
[
  "zh.librella.fi",
  "librella.fi",
  "z-library.sk",
  "z-lib.fm",
  "z-lib.gl",
  "z-library.im"
]
```

正常搜索时不需要打开网页。程序会自动探测可用 eAPI 域名，并把结果缓存一段时间。

需要重新探测时：

```bash
python scripts/zlib_agent.py "测试" --json --refresh-domain
```

ZCode 安全入口不会关闭 TLS 证书校验。证书异常的镜像会直接失败并尝试其他候选。

## 六、凭据

eAPI 需要：

- `remix_userid`
- `remix_userkey`

其中 `remix_userkey` 应当按账号密码处理。

不要：

- 把 userkey 发到聊天里
- 把 userkey 提交到 GitHub
- 在公开截图里展示凭据
- 让 AI 复述 `accounts.json` 或 `credential.json`

现有 `.gitignore` 已排除：

```text
accounts.json
credential.json
credentials/
.env
*.key
*.secret
```

如果缺少凭据，可以由用户本人通过原 CLI 在本地配置；ZCode Skill 只负责提醒，不应该要求用户把密钥贴进对话。

## 七、候选质量建议

查询尽量带上能够区分版本的信息，例如：

```text
书名 + 作者
书名 + 作者 + 第15版
书名 + 作者 + 2024 + 出版社
```

例如：

```bash
python scripts/zlib_agent.py "逻辑学导论 第15版 作者名" --json
```

AI 应优先解释“为什么这个候选更像目标版本”，而不是只说“分最高”。

## 八、原 CLI

原脚本仍然保留：

```bash
python scripts/zlib_cli.py --help
```

它包含搜索、下载、批量书单、额度和账号管理等历史功能。为了兼容已有用户，没有直接删除。

但在当前版本中，ZCode Skill 不会自动使用旧 CLI 的下载功能。后续如果继续维护，建议逐步把 TLS、凭据输出、profile URL 等安全改动迁回旧 CLI，再统一两个入口。

## 九、config.json

当前配置包含：

```json
{
  "library_dir": "books",
  "filename_template": "{title}{author_part}.{ext}",
  "preferred_extension": "epub",
  "preferred_language": null,
  "min_match_score": 25,
  "domains": [
    "zh.librella.fi",
    "librella.fi",
    "z-library.sk",
    "z-lib.fm",
    "z-lib.gl",
    "z-library.im"
  ],
  "timeout": 30,
  "daily_limit": null
}
```

其中 `preferred_extension` 记录项目默认偏好为 EPUB。ZCode 安全入口当前也直接默认 EPUB。

## 十、使用边界

本项目只是客户端工具。请只检索和访问你有权获取的内容，并遵守所在地法律、版权规定和相关服务条款。
